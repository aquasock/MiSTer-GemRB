diff --git a/gemrb/core/Factory.cpp b/gemrb/core/Factory.cpp
index 5b0b4bae..0eab67f4 100644
--- a/gemrb/core/Factory.cpp
+++ b/gemrb/core/Factory.cpp
@@ -20,11 +20,13 @@
 
 #include "Factory.h"
 
+#include <limits>
+
 namespace GemRB {
 
 void Factory::AddFactoryObject(object_t fobject)
 {
-	fobjects.push_back(std::move(fobject));
+	fobjects.push_back({ std::move(fobject), ++useClock });
 }
 
 int Factory::IsLoaded(const ResRef& resref, SClass_ID type) const
@@ -34,8 +36,9 @@ int Factory::IsLoaded(const ResRef& resref, SClass_ID type) const
 	}
 
 	for (unsigned int i = 0; i < fobjects.size(); i++) {
-		if (fobjects[i]->SuperClassID == type) {
-			if (fobjects[i]->resRef == resref) {
+		if (fobjects[i].object->SuperClassID == type) {
+			if (fobjects[i].object->resRef == resref) {
+				fobjects[i].lastUse = ++useClock;
 				return i;
 			}
 		}
@@ -45,7 +48,27 @@ int Factory::IsLoaded(const ResRef& resref, SClass_ID type) const
 
 Factory::object_t Factory::GetFactoryObject(int pos) const
 {
-	return fobjects[pos];
+	fobjects[pos].lastUse = ++useClock;
+	return fobjects[pos].object;
+}
+
+size_t Factory::TrimUnused(size_t maxObjects)
+{
+	size_t removed = 0;
+	while (removed < maxObjects) {
+		auto oldest = fobjects.end();
+		uint64_t oldestUse = std::numeric_limits<uint64_t>::max();
+		for (auto it = fobjects.begin(); it != fobjects.end(); ++it) {
+			if (it->object.use_count() == 1 && it->lastUse < oldestUse) {
+				oldest = it;
+				oldestUse = it->lastUse;
+			}
+		}
+		if (oldest == fobjects.end()) break;
+		fobjects.erase(oldest);
+		++removed;
+	}
+	return removed;
 }
 
 }
diff --git a/gemrb/core/Factory.h b/gemrb/core/Factory.h
index 39763742..9e06c48d 100644
--- a/gemrb/core/Factory.h
+++ b/gemrb/core/Factory.h
@@ -25,7 +25,9 @@
 
 #include "FactoryObject.h"
 
+#include <cstdint>
 #include <memory>
+#include <vector>
 
 namespace GemRB {
 
@@ -40,9 +42,16 @@ public:
 	void AddFactoryObject(object_t fobject);
 	int IsLoaded(const ResRef& resRef, SClass_ID type) const;
 	object_t GetFactoryObject(int pos) const;
+	size_t TrimUnused(size_t maxObjects);
 
 private:
-	std::vector<object_t> fobjects;
+	struct Entry {
+		object_t object;
+		mutable uint64_t lastUse;
+	};
+
+	mutable uint64_t useClock = 0;
+	mutable std::vector<Entry> fobjects;
 };
 
 }
diff --git a/gemrb/core/Game.cpp b/gemrb/core/Game.cpp
index b3ad9ae0..7bd28070 100644
--- a/gemrb/core/Game.cpp
+++ b/gemrb/core/Game.cpp
@@ -27,6 +27,8 @@
 #include "strrefs.h"
 
 #include "DisplayMessage.h"
+#include "AnimationFactory.h"
+#include "CharAnimations.h"
 #include "GameData.h"
 #include "IniSpawn.h"
 #include "Interface.h"
@@ -35,23 +37,179 @@
 #include "MusicMgr.h"
 #include "Particles.h"
 #include "PluginMgr.h"
+#include "Projectile.h"
+#include "ProjectileServer.h"
 #include "ScriptEngine.h"
+#include "ScriptedAnimation.h"
 #include "Spell.h"
 #include "TableMgr.h"
 
 #include "GUI/GameControl.h"
 #include "GameScript/GSUtils.h"
 #include "GameScript/GameScript.h"
+#include "Logging/Logging.h"
+#include "Scriptable/Actor.h"
 #include "Streams/DataStream.h"
 #include "Video/Pixels.h"
+#include "Video/Video.h"
 
 #include <algorithm>
+#include <fstream>
 #include <iterator>
+#include <limits>
+#include <set>
+#include <string>
 #include <utility>
 #include <vector>
 
 namespace GemRB {
 
+namespace {
+
+constexpr size_t PreloadReserveKiB = 80 * 1024;
+
+size_t AvailableMemoryKiB()
+{
+	std::ifstream meminfo("/proc/meminfo");
+	std::string field;
+	size_t value = 0;
+	std::string unit;
+	while (meminfo >> field >> value >> unit) {
+		if (field == "MemAvailable:") return value;
+	}
+	return std::numeric_limits<size_t>::max();
+}
+
+struct AreaPreloader {
+	std::set<std::string> resources;
+	std::set<size_t> projectiles;
+	std::vector<Factory::object_t> pinnedFactories;
+	size_t frames = 0;
+	size_t availableKiB = AvailableMemoryKiB();
+	bool stopped = false;
+
+	bool HasMemory()
+	{
+		availableKiB = AvailableMemoryKiB();
+		if (availableKiB == std::numeric_limits<size_t>::max()) return true;
+		if (availableKiB < PreloadReserveKiB) {
+			gamedata->TrimFactoryCache(32);
+			availableKiB = AvailableMemoryKiB();
+			if (availableKiB < PreloadReserveKiB) {
+				stopped = true;
+				return false;
+			}
+		}
+		return true;
+	}
+
+	void PrecacheAnimation(const Animation* animation, const Holder<Palette>& palette = nullptr)
+	{
+		if (!animation) return;
+		for (Animation::index_t i = 0; i < animation->GetFrameCount(); ++i) {
+			if (!HasMemory()) return;
+			auto frame = animation->GetFrame(i);
+			if (!frame) continue;
+			VideoDriver->PrecacheSpriteWithPalette(frame, palette);
+			++frames;
+		}
+	}
+
+	void PrecacheParts(const CharAnimations::PartAnim* parts, CharAnimations* owner = nullptr, bool shadow = false)
+	{
+		if (!parts) return;
+		for (size_t part = 0; part < parts->size(); ++part) {
+			if (stopped) return;
+			const Holder<Palette> palette = owner ? (shadow ? owner->GetShadowPalette() : owner->GetPartPalette(part)) : nullptr;
+			PrecacheAnimation((*parts)[part].get(), palette);
+		}
+	}
+
+	void PrecacheBAM(const ResRef& resource)
+	{
+		if (resource.IsEmpty() || !HasMemory()) return;
+		std::string key = "bam:" + std::string(resource.c_str());
+		if (!resources.insert(key).second) return;
+		auto factory = gamedata->GetFactoryResourceAs<AnimationFactory>(resource, IE_BAM_CLASS_ID, true);
+		if (!factory) return;
+		pinnedFactories.push_back(factory);
+		for (AnimationFactory::index_t i = 0; i < factory->GetFrameCount(); ++i) {
+			if (!HasMemory()) return;
+			auto frame = factory->GetFrameWithoutCycle(i);
+			if (!frame) continue;
+			VideoDriver->PrecacheSprite(frame);
+			++frames;
+		}
+	}
+
+	void PrecacheVisual(const ResRef& resource)
+	{
+		if (resource.IsEmpty() || !HasMemory()) return;
+		if (!gamedata->Exists(resource, IE_VVC_CLASS_ID, true)) {
+			PrecacheBAM(resource);
+			return;
+		}
+		std::string key = "vvc:" + std::string(resource.c_str());
+		if (!resources.insert(key).second) return;
+		ScriptedAnimation* visual = gamedata->GetScriptedAnimation(resource, false);
+		if (!visual) return;
+		for (Animation* animation : visual->anims) {
+			if (stopped) break;
+			PrecacheAnimation(animation);
+		}
+		if (!stopped && visual->light && HasMemory()) VideoDriver->PrecacheSprite(visual->light);
+		delete visual;
+	}
+
+	void PrecacheEffectResources(const Effect& effect)
+	{
+		const ResRef* refs[] = { &effect.Resource, &effect.Resource2, &effect.Resource3, &effect.Resource4 };
+		for (const ResRef* resource : refs) {
+			if (resource->IsEmpty()) continue;
+			if (gamedata->Exists(*resource, IE_VVC_CLASS_ID, true) ||
+			    gamedata->Exists(*resource, IE_BAM_CLASS_ID, true)) {
+				PrecacheVisual(*resource);
+			}
+		}
+	}
+
+	void PrecacheProjectile(size_t index, unsigned int depth = 0)
+	{
+		if (depth > 2 || !HasMemory() || !projectiles.insert(index).second) return;
+		Projectile* projectile = core->GetProjectileServer()->GetProjectileByIndex(index);
+		if (!projectile) return;
+		PrecacheVisual(projectile->BAMRes1);
+		PrecacheVisual(projectile->BAMRes2);
+		PrecacheVisual(projectile->TravelVVC);
+		for (const ResRef& trail : projectile->TrailBAM) PrecacheVisual(trail);
+		if (projectile->Extension) {
+			PrecacheVisual(projectile->Extension->VVCRes);
+			PrecacheVisual(projectile->Extension->Spread);
+			PrecacheVisual(projectile->Extension->Secondary);
+			PrecacheProjectile(projectile->Extension->ExplProjIdx, depth + 1);
+			PrecacheProjectile(projectile->Extension->FragProjIdx, depth + 1);
+		}
+		delete projectile;
+	}
+
+	void PrecacheSpell(const ResRef& resource)
+	{
+		std::string key = "spl:" + std::string(resource.c_str());
+		if (resource.IsEmpty() || !HasMemory() || !resources.insert(key).second) return;
+		const Spell* spell = gamedata->GetSpell(resource, true);
+		if (!spell) return;
+		for (const Effect& effect : spell->casting_features) PrecacheEffectResources(effect);
+		for (const SPLExtHeader& header : spell->ext_headers) {
+			PrecacheProjectile(header.ProjectileAnimation);
+			for (const Effect& effect : header.features) PrecacheEffectResources(effect);
+		}
+		gamedata->FreeSpell(spell, resource, false);
+	}
+
+};
+
+}
+
 struct HealingResource {
 	ResRef resRef;
 	Actor* caster = nullptr;
diff --git a/gemrb/core/GameData.cpp b/gemrb/core/GameData.cpp
index d343510c..f765b9d0 100644
--- a/gemrb/core/GameData.cpp
+++ b/gemrb/core/GameData.cpp
@@ -372,6 +372,11 @@ Factory::object_t GameData::GetFactoryResource(const ResRef& resName, SClass_ID
 	}
 }
 
+size_t GameData::TrimFactoryCache(size_t maxObjects)
+{
+	return factory.TrimUnused(maxObjects);
+}
+
 Store* GameData::GetStore(const ResRef& resRef)
 {
 	StoreMap::iterator it = stores.find(resRef);
diff --git a/gemrb/core/GameData.h b/gemrb/core/GameData.h
index 1f512e71..0c2a4cba 100644
--- a/gemrb/core/GameData.h
+++ b/gemrb/core/GameData.h
@@ -132,6 +132,8 @@ public:
 
 	/** returns factory resource, currently works only with animations */
 	Factory::object_t GetFactoryResource(const ResRef& resName, SClass_ID type, bool silent = false);
+	/** Drops least-recently-used factory objects that have no external owner. */
+	size_t TrimFactoryCache(size_t maxObjects);
 
 	template<typename T>
 	std::shared_ptr<T> GetFactoryResourceAs(const ResRef& resName, SClass_ID type, bool silent = false)
